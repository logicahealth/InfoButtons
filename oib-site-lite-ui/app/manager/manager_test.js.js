'use strict';

describe('managerModule module', function() {

  beforeEach(module('managerModule'));

  describe('manager controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('ManagerCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});